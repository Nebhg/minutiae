#1. Go to https://www.basketball-reference.com/playoffs/NBA_2023.html
#2. Locate <div class="table_container is_setup" id="div_all_playoffs"></div>
#3  Inside of this div locate the "<tbody>" tag
#4  Inside of the "<tbody>" tag locate the "<tr>" tag with a class of "toggleable open"
#5  Inside of the "<tr>" tag locate the "<table>" tag 
#6  Inside of the "<table>" tag locate the "<tbody>" tag
#7  Inside of the "<tbody>" tag locate the "<td>" 
#8  Inside of the "<td>" tag locate the "<a>" tag
#9  Inside of the "<a>" tag locate the "href" attribute
#10 The "href" attribute will be the link to the box score for the game
#11 Go to the link in the href the link will look something like this "https://www.basketball-reference.com/boxscores/202306010DEN.html"
#12 Locate the <div> tag where id="all_box-MIA-game-basic"
#13 Inside of the <div> tag locate the <div> tag where  the class="table_container"
#14 Inside of the <div> tag locate the <table> tag where the class="sortable stats_table now_sortable"
#15 Inside of the <table> tag locate the <thead> tag
#16 Inside copy the contents of the <thead> tag and store it in a csv file
#17 Inside of the <table> tag locate the <tbody> tag
#18 Inside of the <tbody> copy the contents of the <tbody> tag and store it the same csv as earlier csv file
